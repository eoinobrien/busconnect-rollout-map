data/
__pycache__/
*.pyc
.venv/
.idea/
.vscode/
.pytest_cache/
.playwright-cli/
.claude/
*.png
rail-build.log
# jsdom + friends for the viewer JS tests; only needed locally,
# never deployed.
node_modules/
# QGIS project metadata sidecar files - local to whoever edits the
# manual future-routes GeoJSON, not useful to the deployed map.
*.qmd
